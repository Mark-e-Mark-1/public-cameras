{
  "name": "public-cameras",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "start:pc": "vite preview --host 127.0.0.1 --port 4173 --strictPort --open",
    "validate": "node scripts/validate-catalog.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  }
}
